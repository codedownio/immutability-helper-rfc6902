
name: ci

on:
  pull_request:
  push:
    branches: [master]

jobs:
  test:
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
      with:
        node-version: 14
    - run: npm ci
    - run: npm test
